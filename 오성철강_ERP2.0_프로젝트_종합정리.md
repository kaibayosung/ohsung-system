# 오성철강 스마트 ERP 2.0 — 프로젝트 종합 정리

최초 작성: 2026-07-31 · 최신 갱신: 2026-08-26 · 이 문서는 프로젝트(Project) 등록용으로, 지금까지 진행된 전체 작업 내용을 최대한 상세히 정리한 것입니다.

---

## 1. 프로젝트 개요

- **회사**: 오성철강(1983년 설립, 44년) — 자동차·건축용 정밀 탄소강 코일센터. 슬리팅(코일 폭 절단)·레벨링(평탄화) 공정 전문.
- **목표**: 기존 외부 ERP(그린ERP/그린피웹)를 그대로 유지하면서, 그 위에 실데이터 기반의 자체 "스마트 ERP 2.0"을 구축 — 경영보고 자동화, 현장 업무 디지털화, 그리고 AI를 활용한 신규 서비스 모델(가공 하청업체 → 통합 서비스 제공업체)로의 전환.
- **레포지토리**: 로컬 `C:\Users\USER\Documents\개발\ohsung-system`, GitHub(`kaibayosung` 계정) 연동.
- **배포**: Vercel — `https://ohsung-system.vercel.app` (메인 ERP), `/portal` (고객사 포털), `/separator` (세퍼레이터 태블릿 키오스크), `/warehouse-3d.html` (코일창고 3D 뷰어). 8월부터 서브도메인을 `smart.osungsteel.co.kr`(실제 도메인, 닷네임코리아)로 전환. Vercel 장애 대비 백업 경로로 `192.168.0.9` 로컬 서버 + nginx + Cloudflare Tunnel 구성도 `deploy/local-server/`에 추가해둠(고객사 포털 외부 운영용).
- **데이터베이스**: Supabase Postgres (project_id: `mnlnbwcbgfgftdgawanh`).
- **기술 스택**: React 19 + Vite(멀티 엔트리) + React Router + Supabase JS + Chart.js/Recharts + Supabase Edge Functions(Deno) + pg_cron.

---

## 2. 시스템 아키텍처

### 2.1 앱 구조 (Vite 멀티 페이지)
`vite.config.js`에 3개 엔트리 포인트가 정의되어 있고, `vercel.json`이 클린 URL로 라우팅합니다.

| 엔트리 | URL | 용도 |
|---|---|---|
| `index.html` (main) | `/` | 메인 ERP 2.0 (직원용) |
| `portal.html` | `/portal` | 고객사 포털 (거래처 로그인) |
| `separator.html` | `/separator` | 슬리터2 세퍼레이터 태블릿 키오스크 |

`public/` 폴더 파일은 그대로 정적 서빙되므로, `warehouse-3d.html` 같은 단일 HTML 파일도 별도 라우팅 설정 없이 바로 URL로 노출됩니다.

### 2.2 그린ERP 연동 (핵심 데이터 파이프라인)
기존 외부 ERP "그린피웹(greenpweb.co.kr)"의 데이터를 Supabase Edge Function으로 주기적으로 끌어와 자체 테이블에 동기화합니다.

- **Edge Functions** (`supabase/functions/`):
  - `greenp-sync-v2` — 작업지시서(joborders), 생산(production), 입출고(inbound/outbound), 재고(inventory), 매출채권(receivables) 등 핵심 테이블 동기화
  - `greenp-joborder-detail-sync` — 작업지시서 상세(가공규격/process_rule 포함) 동기화
  - `greenp-unshipped-sync` — 미출고 현황 동기화 (그린ERP 원본 API 직결 방식)
  - `enfax-inbound-daily` — 엔팩스(FAX) 수신함 자동 동기화

- **크론 스케줄** (pg_cron, 업무시간 08:00~17:40 KST 기준 10분 간격으로 통일됨):
  - `greenp-sync-v2-morning/day/evening`
  - `greenp-joborder-detail-morning/day/evening`
  - 스케줄은 UTC 기준이라 `*/10 0-7 * * *`(day), `0,10,20,30,40 8 * * *`(evening), `0,10,20,30,40,50 23 * * *`(morning) 형태로 등록되어 있음.

- **핵심 테이블**: `greenp_joborders`(작업지시 목록), `greenp_joborder_detail`(가공규격 등 상세), `greenp_production`, `greenp_inbound`, `greenp_outbound`, `greenp_inventory`, `greenp_receivables`, `greenp_unshipped`, `greenp_customers`(뷰), `greenp_sync_logs`(동기화 로그).

- **중요한 동작 특성**: `greenp-sync-v2-*` 계열은 **오늘 날짜 데이터를 매 10분마다 통째로 다시 채우는 방식**이라, 수동으로 넣은 임시/데모 데이터가 다음 동기화 때 사라질 수 있습니다. (참고: `greenp_joborder_detail`은 별도 동기화 방식이라 상대적으로 데모 데이터가 덜 지워짐.)

### 2.3 데이터베이스 계정/보안
- Supabase Auth 기반, `staff_users`(직원, 역할별 접근제어), `customer_users`(거래처 고객사 포털 계정 — 이메일 없이 아이디+비밀번호 가입 지원, 내부적으로 `{아이디}@ohsungportal.local`로 변환).
- RLS 정책: `is_staff()`(직원 전체 조회), `is_my_company(company_name)`(고객사는 본인 거래처 데이터만 조회) 두 가지 패턴을 대부분의 그린ERP 동기화 테이블에 적용.

---

## 3. 개발 타임라인 (git 커밋 기준, 총 232건)

git 로그 전수 분석 결과 (root 커밋부터 최신까지, `kaibayosung` 단일 저자):

| 단계 | 기간 | 커밋 수 | 핵심 내용 |
|---|---|---|---|
| 1단계 · 초기 시스템 구축 | 2026-01-28 ~ 02-01 | 47건 | 최초 배포, 작업일보·일계표 중복방지, CEO 리포트(일일/월간) DB연동 |
| 2단계 · 실데이터 연동·기능 확장 | 2026-07-16 ~ 07-21 | 46건 | 지출결의서 신설, 그린ERP 10분 자동동기화 가동, 영업워크플로우 5역할, 엔팩스 연동, 고객사 포털, 입출고 FAX 리포트, 스크랩매출 |
| 3단계 · 안정화·보안 강화 | 2026-07-22 ~ 07-25 | 32건 | 코일 자동배정, 계정관리(권한별 접근제어), 고객사포털 로그인 버그 근본수정, 확정 월고정비 반영 |
| 4단계 · 연구실 프로토타입·신규서비스 | 2026-07-26 ~ 07-30 | 31건 | 세퍼레이터 키오스크 실배포, 그린ERP 동기화 치명적 버그 다수 수정(v13~v18), 창고3D/장애분석 연구실 추가 |
| 5단계 · 연구실 대량 확장·현장 실전 배포 | 2026-07-31 ~ 08-26 | 76건 | 레벨링 라인 통합관제(NMS)·통합 타임라인·HMI 미러·공정 모니터링·코일 상세분석 등 실데이터 연동 화면 다수 신설, 슬리팅2 AI 헬퍼(사무실용/현장용/모바일 한·영), 작업현황 대시보드 1~3 순차 고도화, 고객사 챗봇·주문현황 트래커·계좌손익·입금확인 대사 등 고객·경영 지원 도구 추가, 지출결의서 AI 초안 업로드(이미지/엑셀/붙여넣기), 타임존 이중보정 버그 전체 화면 일괄 수정, 테이퍼 텐션 컨트롤러(PR-DTC-3100) 카메라+OCR 원격 모니터링 현장 실전 착수(마그네틱 거치대 안정화, 픽셀 단위 재보정, Supabase 실시간 연동) |

> 참고: 1단계(01-28~02-01)와 2단계(07-16) 사이 약 5.5개월의 공백이 있습니다. git 최초 커밋(`오성철강 시스템 최종 배포 준비`, 01-28 15:46) 시점에 이미 "최종 배포 준비"라는 메시지가 붙어있고, 파일들의 실제 생성 시각도 zip 백업 복원 시점(01-29)에 몰려있어, **이 저장소 기준으로 확인 가능한 가장 이른 기록이 01-28**이라는 의미입니다. 그 이전 실제 개발이 언제 시작됐는지는 git 기록만으로는 알 수 없습니다.

(상세 날짜별 패치 표는 별도 산출물 `오성철강_ERP2.0_패치이력보고.pptx` 참고)

---

## 4. 전체 기능 / 메뉴 구조

### 4.1 워크플로우 관리 (운영자용 · 현장 실무)
- 입고 FAX 리포트 — 입고현황을 그린ERP 원본 양식 그대로 PDF/FAX 발송 (로고·확인도장 포함)
- 작업일보 — 벌크저장, 중복 데이터 자동 필터링, 인라인 수정

### 4.2 매출 관리 (운영자·경리용)
- 영업 워크플로우 — 5개 역할(대표/관리자/영업/지게차기사/고객사) UI로 재구성된 주문~배차~코일확정 흐름
- 스크랩 매출 — 계량증명서 기반 포장판고철/고철 매출 등록, 일계표에 자동 반영

### 4.3 비용 관리 (경리용)
- 지출결의서 — 목록/작성/출력, 결재함/결재완료 처리, 계좌 관리, 결재라인(과장 포함), 정기지출 초안, 계정과목·계좌잔액·특수이체 대시보드
- 지출결의서 AI 초안 업로드 (8월 신설) — 영수증/명세서 이미지(`expense-doc-ocr` Edge Function으로 OCR), 엑셀 파일, 엑셀에서 복사한 표 붙여넣기(Ctrl+V) 세 가지 방식으로 다건 항목을 한 번에 인식해 초안 생성. 합계 행 오인식 등 실사용 중 발견된 파싱 버그 다수 수정, 결재건별 분리 저장 지원
- 지출결의서 출력 — 시안 기반 리디자인 여러 차례 (열 너비/정렬/폰트 크기 조정, 비고 말줄임 처리, 짝수행 음영 등 실사용 피드백 반영)
- 일계표 — 매출 자동 반영, 인라인 수정

### 4.4 대표님 경영보고 (관리자 전용)
- 데일리 리포트 / 월간 분석 / 대표님 브리핑(CEOMonthlyReport, 확정 월고정비 반영) / 카톡용 일일 요약(CEODailyKpiPage) / 접속 로그

### 4.5 고객사 포털 (`/portal`, 거래처 전용 로그인)
- 재고현황(그린ERP 실데이터, 두께별 파이그래프), 작업내역(FAX 전송 버튼), 출고내역, 입고내역, 미출고 리스트
- 거래처명 검색(전체 55개사), 기간 선택 조회, 분석 그래프(두께별/기간별/작업유형별), 인쇄/PDF 저장
- 로그인 안정성 이슈(세션 락 경쟁상태) 근본 원인 수정 완료

### 4.6 계정 관리 (관리자 전용)
- `staff_users` 기반 역할별(관리자/일반직원) 접근 제어, 비밀번호 변경/재설정, 계정 삭제 기능(8월 추가)

### 4.7 오성철강 연구실 (AI 신규제안 프로토타입 미리보기)
카드형 대시보드, 카테고리별로 자동 그룹핑됩니다. 실제 배포된 서비스는 "현장 배포중" 배지가, 실데이터로 동작하는 것은 "실데이터 연동" 배지가 붙습니다. 8월 한 달 사이 항목이 8개→24개로 크게 늘었고, "고객사 시연"·"경영·재무" 카테고리가 새로 생겼습니다.

| 카테고리 | 항목 | 상태 |
|---|---|---|
| 주문접수 자동화 | 신규서비스 통합흐름 v2, OCR 문서인식, 카카오톡 주문접수, FAX 작업요청서 접수, 현장 코일확정 | 프로토타입 |
| 생산현장 도구 | 세퍼레이터 셋팅 계산기(사무실용) | 프로토타입 |
| 생산현장 도구 | 세퍼레이터 태블릿 키오스크 | **현장 배포중** |
| 생산현장 도구 | 작업현황 대시보드 / 대시보드 2 / 대시보드 3 (레벨링·슬리팅1·슬리팅2, 라인별 보드·완료예측·로그인없는 공개링크로 순차 고도화) | **실데이터 연동** |
| 생산현장 도구 | 코일 작업 상세 분석, 레벨링 코일 상세분석 | **실데이터 연동** |
| 생산현장 도구 | 레벨링 공정×ERP 통합 대시보드, 레벨링 공정 현황 모니터링, 레벨링 라인 통합 관제(NMS), 레벨링 통합 타임라인, 레벨링 실시간 전체 데이터(HMI 미러) | **실데이터 연동** |
| 생산현장 도구 | 레벨링 PLC 상세 조회 | **실데이터 연동** |
| 생산현장 도구 | AI 헬퍼 (슬리팅2/지난작업보기, 작업용), AI 헬퍼 모바일(한국어/English) | **실데이터 연동** |
| 생산현장 도구 | 테이퍼 텐션 컨트롤러 원격 모니터링(PR-DTC-3100, 카메라+OCR) | 샘플 → **현장 캘리브레이션 진행중** (2절 참고) |
| 생산현장 도구 | 레벨링 작업 가이드(외국인 근로자용) | 샘플 |
| 생산현장 도구 | 테이퍼 텐션 데이터 수신 확인(DB 실시간 적재 모니터) | **실데이터 연동** |
| 영업 지원 | 영업대상 고객사 리스트 | **실데이터 연동** |
| 고객사 시연 | 고객 주문 현황 트래커, (주)대한강재 챗봇(작업현황·미수금·출고여부, Claude 자연어 응답) | **실데이터 연동** |
| 창고 관리 | 코일창고 3D 뷰어·출고관리 | **현장 배포중** |
| 설비 진단 | 장애 원인 분석(AI) | 샘플 (실제 슬리터 사고 사례 기반) |
| 경영 · 재무 | 계좌 손익 통합 대시보드(통장+그린ERP 매출 합산 월손익), 거래명세서 입금 확인 대사 | **실데이터 연동** (통장 내역은 미저장) |

### 4.8 내부 시스템 링크
- **ERP 2.0 현장 배포 서비스**: 세퍼레이터 태블릿 키오스크(`/separator`)
- **자체 개발 시스템(사내 서버)**: 슬리팅 대시보드, 작업선택 PAD(슬리팅1/2·레벨링), PLC 데이터 통합저장, 품질측정시스템(SAIGE VIMS — 플랫폼 통합중)
- **외부 연동 시스템**: 그린피웹 ERP, enFax, 슬리팅2 장비 운용 실시간 NMS 대시보드

---

## 5. 그린ERP 연동 관련 발견·수정된 주요 버그

실사용 데이터로 검증하는 과정에서 다음과 같은 데이터 정합성 문제들을 발견하고 수정했습니다.

1. **작업지시서 동기화 타임존 버그** — 최초 크론 타임존 설정 오류로 최신 작업지시서가 안 보이던 문제.
2. **출고 응답 잘림 버그(v13)** — 거래처 필터 없이 조회하면 API 응답이 3건으로 잘리는 문제. 재백필 진행.
3. **작업상세 동기화 upsert 중복키 오류(v6)** — 넓은 기간 백필 시 발생.
4. **enfax-inbound-daily 페이지네이션 버그(v15)** — `greenp_outbound` 1000행 캡으로 미출고 건수가 과다 표시되던 문제.
5. **미출고현황 개념 재정의** — 그린ERP 원본 API 직결 방식으로 전면 교체, 5회 이상 교차검증으로 정합성 확인.
6. **전체 엔드포인트 잘림방지+재시도 로직(v15~v18)** — 그린ERP 응답 전수조사 후 모든 동기화 함수에 재시도/잘림방지 로직 일괄 적용.
7. **세퍼레이터 키오스크 `todayKST()` 타임존 이중보정 버그** (2026-07-30 발견·수정) — `now.getTime()`은 이미 타임존과 무관한 절대시각인데 `getTimezoneOffset()`을 추가로 더해 이중 보정되던 문제. 기기가 KST(UTC+9)일 때 보정이 서로 상쇄되어 실제로는 UTC 날짜가 나왔고, 그 결과 **한국시간 00:00~08:59 사이에는 실제 날짜보다 하루 이전 날짜로 조회**되는 버그였음. `SeparatorKiosk.jsx`에서 수정 완료.
8. **같은 이중보정 버그가 레벨링/AI헬퍼 화면 전반에도 존재** (2026-08-26 발견·수정) — 세퍼레이터에서 고친 것과 동일한 패턴의 버그가 레벨링·AI 헬퍼 관련 화면 전체에 퍼져 있어 시각이 9시간 밀려 표시되고 있었음. 전체 화면 일괄 점검 후 수정.

> 데모/테스트 관련: `greenp_joborders`는 크론이 10분마다 당일 데이터를 통째로 새로고침하므로, 수동 삽입한 데모 행은 다음 동기화 때 사라질 수 있음을 여러 차례 확인. 급한 시연이 필요할 때는 ①크론을 일시정지(`cron.alter_job(id, active => false)`)하거나 ②이미 존재하는 실제 행의 상태값만 바꾸는 방식을 사용함.

---

## 6. AX(AI Transformation) 전략 트랙 — 별도 보고 시리즈

ERP 2.0 개발과 별개로, 대표님 보고용 AI 도입 전략 자료를 지속적으로 고도화해왔습니다.

- **오성철강_AI도입전략.pdf / AI도입맵.pptx / AI도입계획안(v2).docx** — 최초 AI 전략 제안 문서군 (카카오톡 주문접수, OCR, 코일관리 QR입고 등 신규 서비스 구상)
- **오성철강_보유데이터현황(v2).pptx, SAIGE데이터분석리포트.docx, 데이터통합발전전략(.pptx/.docx)** — 보유 데이터 자산(그린ERP 34,707건 · 자체ERP2.0 14,513건 · 설비PLC 1,823,733건 · SAIGE VIMS 1,953건, 총 1,872,953건) 정리 및 데이터 신뢰성 검증 결과
- **오성철강_AX현황및계획_v3_final.pptx** (16장) — 가장 종합적인 대표님 보고용 발표자료. 오성철강 소개 → 현황(3대 위기: 장비노후화/기능인고령화/고객서비스확대) → 1차도전(장비도입 실패) → AI 1~3차 도전(SAIGE VIMS+AI Helper, RPA+대시보드, 고객포털+카카오톡/OCR) → 자체개발 서비스 10종 상세 → 서비스맵 인포그래픽(자체개발/외부도입 구분) → 레벨링 라인 품질측정(반월시화 AX 사업, 총사업비 5,500만원) → 데이터 보유·검증 현황 → 별첨(SAIGE VIMS 데이터 상세) → 통합 인사이트(Coil ID) → **비전(AI는 단위기술이 아닌 사업모델 전환의 핵심 — 가공 하청업체→통합 서비스 제공업체)** → **신규서비스모델(기존서비스+AI=새로운서비스)** → 로드맵(Step1~3) → 클로징. 이후 `_v3_final_수정.pptx`로 추가 보완.
- **오성철강_ERP2.0_패치이력보고.pptx** — git 커밋(당시 156건)을 4단계로 정리한 대표님 보고용 패치 이력 표
- **`발표자료/` 폴더 신설 (8월)** — AX 관련 산출물을 별도 폴더로 정리:
  - 오성철강_AX여정_스토리라인(안).docx/pdf, 오성철강_매출성장4단계_스토리라인(안).docx/pdf — 보고 스토리라인 초안
  - 오성철강_산업통상자원부_건의문_모델공장지정요청.docx — 정부(산업통상자원부) 모델공장 지정 요청 건의문
  - 오성철강_슬리팅2_코일분석_AI헬퍼_기능설명서.docx — AI 헬퍼 기능 설명서
  - CSF9700B_작업분석보고서.docx/pdf (영문판 포함) — 특정 코일/작업 건 분석 보고서

---

## 7. 생성된 문서/산출물 전체 목록 (날짜순)

워크스페이스(`C:\Users\USER\Documents\개발\ohsung-system`)에 실제로 저장되어 있는 파일들입니다.

**7/16 (지출 시스템 초기 기획)**
- 오성철강_월별고정비_대표님보고_최근실적.pdf / _통합.pdf / _3p.pdf
- 오성철강_지출사전결재시스템_기획서.docx / (은행명수정).docx

**7/17~7/19 (그린ERP 분석 및 스마트ERP2.0 통합 기획)**
- 지출결의_시스템_개선_UI시나리오.pdf
- 오성철강_스마트ERP2.0_통합기획안.docx
- 오성철강_그린피ERP분석_스마트ERP수용방안.docx
- 오성철강_그린피ERP_프레젠테이션.pptx
- 오성철강_스마트ERP2.0_UI시나리오_기획안.pptx
- 오성철강_스마트ERP2.0_전체화면_와이어프레임.pdf
- 오성철강_스마트ERP2.0_실데이터_테스트결과보고서.docx
- greenp_data_structure_report.docx
- 오성철강_스마트ERP2.0_신규기획서.docx
- 오성철강_전체워크플로우_상세기능기획서.docx
- 오성철강_코일발주출고_화면기획서.docx
- 오성철강_역할별화면고도화_회의록및기획서.docx
- 오성철강_스마트ERP2.0_완전이관_기획서.docx

**7/25 (AI 도입 전략 및 코일관리 기획)**
- 오성철강_AI도입전략.pdf
- 오성철강_AI도입맵.pptx
- 오성철강_AI도입_문제점진단및해결방안.docx
- 오성철강_AI도입계획안.docx / _v2.docx
- 오성철강_코일관리서비스_기획서.docx / _v2.docx / _v3.docx

**7/26 (데이터 정리 및 프로세스 확정)**
- 오성철강_확보가능데이터_정리.docx
- 오성철강_주문접수자동화_상세시나리오.docx
- 오성철강_이해관계자검토회의_최적프로세스확정.docx

**7/28 (그린ERP 동기화 문제 분석 및 데이터 검증)**
- 그린ERP_ERP2.0_동기화_문제점분석_해결방안.docx
- 오성철강_영업대상고객사리스트_기획서.docx
- 오성철강_보유데이터현황.pptx / _v2.pptx
- 오성철강_확보가능데이터_정리_v2.docx / _v3.docx
- 오성철강_SAIGE데이터분석리포트.docx
- 오성철강_데이터통합발전전략보고서.docx / .pptx

**7/29~7/30 (AX 종합 발표자료 및 패치 이력)**
- 오성철강_데이터서비스_요약.pptx
- 오성철강_AX현황및계획.pptx → _v2.pptx → _v3.pptx → **_v3_final.pptx** (최종본)
- 오성철강_ERP2.0_패치이력보고.pptx

**8월 (연구실 확장 + AX 보고자료 보완)**
- 발표자료/오성철강_AX여정_스토리라인(안).docx / .pdf
- 발표자료/오성철강_매출성장4단계_스토리라인(안).docx / .pdf
- 발표자료/오성철강_산업통상자원부_건의문_모델공장지정요청.docx
- 발표자료/오성철강_슬리팅2_코일분석_AI헬퍼_기능설명서.docx
- 오성철강_AX현황및계획_v3_final_수정.pptx
- CSF9700B_작업분석보고서.docx / .pdf / _EN.pdf

**웹 배포 산출물 (workspace 루트 및 public/)**
- separator_slitter2_tablet_flow_v1.html / v2.html (초기 세퍼레이터 시안)
- 오성철강_ERP2_동작프로토타입.html
- warehouse-3d.html (코일창고 3D 뷰어, `public/`에 배포되어 실제 URL로 서빙 중)
- taper-tension-monitor.html, taper-tension-guide.html, taper-tension-ingestion-status.html, leveling-plc-viewer.html, leveling-integrated-timeline.html, leveling-realtime-hmi.html, work-status-3.html, ai-helper-mobile-kr.html, ai-helper-mobile-en.html (8월 신설, 모두 `public/`에서 서빙 중)

---

## 8. 최근 처리한 이슈

### 8.1 (2026-07-30~31)

1. **세퍼레이터 키오스크 데이터 없음 문제** — 원인은 두 가지가 겹쳐 있었음:
   - (a) 그린ERP 원본에 당일 슬리터2 작업지시서가 아직 등록되지 않은 시점이 있었음 (정상 — 데이터 자체가 없던 것)
   - (b) `todayKST()` 함수의 타임존 이중보정 버그로, 한국시간 오전 9시 이전에는 하루 전 날짜로 조회하고 있었음 (코드 버그)
   - 임시 조치: 실제 그린ERP에 등록된 당일 작업지시서 8건(대한강재, SLITING2)을 정확한 날짜로 직접 반영, 데모용으로 임시 삽입했던 가짜 데이터는 실데이터 도착 후 정리함.
   - 근본 조치: `SeparatorKiosk.jsx`의 `todayKST()` 함수 수정, 이후 push 완료.
   - 동기화 크론(`greenp-sync-v2-morning/day/evening`)은 시연 중 데이터가 지워지지 않도록 일시정지해두었다가, 실데이터 반영 후에는 재개가 필요함 — **재확인 필요**.

2. **ERP 2.0 초기 구축 일자 재확인** — git root 커밋(2026-01-28 15:46, "오성철강 시스템 최종 배포 준비")이 이 저장소 기준 가장 이른 기록임을 reflog·파일 mtime까지 교차 검증. 그 이전 실제 개발 시작일은 git 기록만으로는 확인 불가.

### 8.2 (2026-08-26) — 테이퍼 텐션 컨트롤러 OCR 현장 실전 착수

PORA PR-DTC-3100 타이퍼 텐션 컨트롤러는 통신 포트가 없어, 폰 카메라로 LCD를 촬영하고 OCR로 값을 읽어 Supabase에 저장하는 방식(`ocr_monitor.py`, 오성철강 저장소 밖 별도 스크립트)으로 접근했습니다. 오늘 하루 동안 진행된 내용:

1. **거치대 진동 문제 → 마그네틱 마운트로 전환**: 처음 쓴 다관절 거치대는 설비 진동으로 계속 위치가 틀어져, 앵글브라켓+볼트 고정 또는 마그네틱 마운트(Z Fold4 맥세이프 링케이스 + 강한 차량용 거치대)를 검토·추천했고, 최종적으로 마그네틱 거치대로 해결함(사용자 확인).
2. **좌표 캘리브레이션을 픽셀 단위 실측으로 재작업**: 화면 자동 감지 실패 시 쓰는 기본 박스(`DEFAULT_SCREEN_BOX`)와 5개 필드(출력%/두께/시작경/장력율/장력설정)의 상대 좌표(`REL_*_BOX`)가 여러 차례 어긋났음. 실제 캘리브레이션 사진을 OpenCV로 픽셀 단위 분석(색상 마스크로 각 값의 정확한 bbox 추출)해 화면 박스 우측 경계를 넓히고 5개 필드 좌표를 전부 재계산, 실제 사진에 그려서 검증까지 완료.
3. **Supabase 실시간 연동**: `taper_tension_readings` 테이블(RLS: anon은 조회만, 저장은 service_role 키로만 가능)에 매 캡처 주기마다 값을 저장하도록 `save_reading_to_supabase()` 추가. 저장 여부는 `SUPABASE_SERVICE_ROLE_KEY` 환경변수 유무로 제어(cmd 창마다 새로 설정해야 하는 게 실사용 중 반복적으로 누락되는 지점으로 확인됨).
4. **OCR 신뢰도/오독 관련 버그 2건 수정**:
   - 첫 인식이 잘못되면(예: "2202"를 "2"로 오독) 이후 실제 값이 들어와도 "너무 급격한 변화"로 계속 반려되어 틀린 값에 영구히 갇히는 문제 → 같은 새 값이 3회 연속 관측되면 실제 값 변화로 보고 승격하는 로직 추가.
   - 반사광/글레어로 신뢰도가 매우 낮은(5~11%) 상태에서도 자릿수만 맞으면 그대로 DB에 저장되던 문제 → 신뢰도 50% 미만은 값 자체를 무시하도록 게이트 추가. 색상 마스크만으로 인식 실패 시 명암 대비 기반 이진화 폴백도 추가.
5. **배포 누락 발견**: 연구실 메뉴에는 "테이퍼 텐션 데이터 수신 확인" 카드가 이미 연결돼 있었는데, 정작 `public/taper-tension-ingestion-status.html` 파일이 한 번도 git에 커밋된 적이 없어 Vercel에서 404가 발생했음. 커밋·푸시로 해결.

**남은 이슈**: 신뢰도 게이트를 추가한 새 버전으로 재검증 필요(신뢰도가 낮은 근본 원인이 반사광인지 해상도/압축인지 아직 확정 못함), Z Fold4 폴드 접었을 때 카메라 스트림이 끊기는 문제는 미해결(자석 마운트 도입으로 펼친 상태 고정은 해결됨).

---

## 9. 현재 진행 중 / 미해결 항목

- **그린ERP 입고전표 등록(쓰기) 엔드포인트 조사** — 현재는 조회(읽기) 전용 동기화만 구현되어 있고, ERP 2.0에서 입고전표를 직접 등록(쓰기)하는 기능은 아직 조사 단계.
- **동기화 크론 재개 여부 확인** — 데모 목적으로 일시정지한 `greenp-sync-v2-*` 크론을 원상복구해야 함.
- **테이퍼 텐션 OCR 신뢰도 검증** — 8/26 신뢰도 게이트(50% 미만 무시) + 명암대비 폴백 추가한 새 버전을 현장에서 재실행해, 낮은 신뢰도의 근본 원인(반사광/화질/초점)을 확인하고 실제 값이 안정적으로 쌓이는지 확인 필요.
- **Z Fold4 폴드 접힘 시 카메라 스트림 끊김** — 마그네틱 마운트로 펼친 상태 고정은 해결됐으나, 접었을 때도 후면 카메라로 계속 촬영하는 방법 자체는 아직 미해결(대안: DroidCam 등 다른 스트리밍 앱 검토 가능).
- **참고**: 샌드박스에는 GitHub 인증정보가 없어 이 세션에서는 로컬 커밋까지만 가능하고, 실제 원격 반영(`git push origin main`)은 사용자가 직접 실행해야 함 — 지금까지는 매번 잘 반영되고 있음.

---

## 10. 참고 정보

- Supabase project_id: `mnlnbwcbgfgftdgawanh`
- 메인 URL: `https://ohsung-system.vercel.app` (실도메인: `https://smart.osungsteel.co.kr`)
- 고객사 포털: `/portal`
- 세퍼레이터 키오스크: `/separator`
- 코일창고 3D 뷰어: `/warehouse-3d.html`
- 레벨링 통합 타임라인: `/leveling-integrated-timeline.html` · 레벨링 라인 통합 관제(NMS): 연구실 내부 화면 · 레벨링 실시간 HMI 미러: `/leveling-realtime-hmi.html`
- 테이퍼 텐션 데이터 수신 확인: `/taper-tension-ingestion-status.html`
- Vercel 장애 대비 백업: `192.168.0.9` 로컬 서버(nginx) + Cloudflare Tunnel (`deploy/local-server/`)
- Git: 로컬 커밋은 가능하나 원격 push는 사용자가 직접 `git push origin main` 실행 필요 (샌드박스에 자격증명 없음)
